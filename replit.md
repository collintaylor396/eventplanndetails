# Project

This repository is a self-contained static web page. It has no package dependencies or backend service.

## Run

Serve the project root on port 5000:

```sh
python3 -m http.server 5000 --bind 0.0.0.0
```

The Replit **Start application** workflow runs this command and opens the web preview.